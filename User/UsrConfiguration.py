import config
from config import usr

def on_error():
  print("A configuration error has been detected and we will get right to it.")
  printScreen("A configuration error has been detected and we will get right to it.")
  
if gui.actvt():
  showGUI(option1("Report to AEGiS Corperation®"),nop(3))
                  printScreen("Thank you for reporting an error.")
                  print("Reported.")
    option2("Cancel",nop(3))
            printScreen("The action has been cancelled.")
      print("The action has been cancelled.")
